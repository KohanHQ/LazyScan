# Secrets — never commit a real .env (only .env.example is tracked)
.env
.env.*
!.env.example

# Local dev stack (stands in for shared postgres/redis + R2) — never deploy
docker-compose.override.yml

# Node / Bun deps + build output
node_modules/
dist/
build/

# Go build output
*.exe
*.test
*.out
/image/image
/mail/mail

# Logs + OS cruft
*.log
.DS_Store
.coderabbit.yaml

# internal planning docs
AGENTS.md
docs/
